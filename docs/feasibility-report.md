# LangSmith Hyper-Granular Trace Querying Feasibility

## Answer

Yes, we can accomplish the feature in the public open-source LangSmith/LangChain materials that are available today, with one boundary:

- We can ship a practical implementation in the public `langchain-ai/langsmith-cli` repository and document it in the public `langchain-ai/docs` repository.
- We cannot implement native hosted-LangSmith backend indexing, server-side query semantics, or hosted UI behavior from public code, because there is no public `langchain-ai/langsmith` app/backend repository.

The implemented OSS path exposes maximum practical granularity through the CLI: request all selectable run fields, preserve raw hydrated run JSON, derive trace depth for output and summaries, filter locally over arbitrary JSON paths, and find traces whose tree contains matching agent/subagent spans. Current LangSmith system metadata also supports exact run-depth filtering through `ls_run_depth`.

## Public Repositories Checked

- `langchain-ai/langsmith-cli`: public, current CLI surface for querying LangSmith from a terminal.
- `langchain-ai/langsmith-sdk`: public SDKs; the Python/JS/Go clients expose run querying, selected run fields, `trace_filter`, `tree_filter`, and parent-run filtering.
- `langchain-ai/docs`: public current docs repository. `langchain-ai/langsmith-docs` exists but is deprecated and points here.
- `langchain-ai/langchain` and `langchain-ai/langgraph`: public framework repos, useful for tracing integrations but not the LangSmith query service itself.
- `langchain-ai/langsmith`: not publicly available/resolvable via GitHub.

## Query Granularity Available Publicly

Server/API-side:

- `filter`: structured LangSmith filter DSL for run fields.
- `trace_filter`: applies to the root run in a trace tree.
- `tree_filter`: applies to other runs in the trace tree, including child/sibling runs.
- `parent_run_id`: returns direct children of a run.
- `ls_run_depth`: system metadata that supports exact run-depth filtering through the filter DSL.
- `select`: controls which run fields are returned.

Run hierarchy fields:

- `trace_id`: groups runs into a trace.
- `parent_run_id`: immediate parent.
- `parent_run_ids`: all parents; length gives zero-based depth.
- `dotted_order`: sortable hierarchy encoding; dot count gives depth when `parent_run_ids` is unavailable.
- `child_run_ids` / `direct_child_run_ids`: child references.

Client-side hydrated querying added in the CLI patch:

- `--include-all`: requests every current public `langsmith-go` `RunQueryParamsSelect` field and emits raw run JSON.
- `--min-depth` / `--max-depth`: provide client-side range filtering and derived depth output; exact depth can also be expressed server-side with `ls_run_depth`.
- `--agent-type`: filters `extra.metadata.ls_agent_type`, including OpenAI Agents SDK `subagent` spans where that metadata is present.
- `--field path=value`: repeatable exact-match filter over hydrated JSON paths, including nested objects and numeric array indices.
- On `trace list` and `trace export`, local depth, agent type, and field filters match any run in each candidate trace tree. This supports trace-level queries that combine depth-like selection with fields that are only available after hydration.

## Implemented Artifacts

CLI implementation:

- Branch: `feature/hyper-granular-trace-querying`
- Commit: `49f32d2 Add hyper-granular LangSmith run querying`
- Patch: `upstream/patches/0001-add-hyper-granular-langsmith-run-querying.patch`
- PR body draft: `upstream/langsmith-cli-pr-body.md`

Docs companion:

- Branch: `docs/langsmith-cli-hyper-granular-querying`
- Commit: `8b70bd06 Document granular LangSmith CLI run querying`
- Patch: `upstream/patches/0001-document-granular-langsmith-cli-run-querying.patch`
- PR body draft: `upstream/langchain-docs-pr-body.md`

## Verification

CLI verification run from `langsmith-cli/`:

```bash
GOCACHE=/tmp/go-build GOMODCACHE=/tmp/go-mod-cache go test -count=1 ./...
GOCACHE=/tmp/go-build GOMODCACHE=/tmp/go-mod-cache go vet ./...
GOCACHE=/tmp/go-build GOMODCACHE=/tmp/go-mod-cache CGO_ENABLED=0 go build -o /tmp/langsmith-cli-build ./cmd/langsmith
GOCACHE=/tmp/go-build GOMODCACHE=/tmp/go-mod-cache golangci-lint run
git diff --check
```

The CLI test coverage includes:

- Unit tests for server-side query params, all-field selection, derived depth/agent fields, local depth/agent/path filters, and trace-tree local filter splitting.
- HTTP-level command tests proving that local depth, agent type, and arbitrary field filters on `trace list` can match any run in each hydrated trace tree.
- Fixture-backed e2e/golden tests generated by running the real Cobra root command and real LangSmith SDK client against a deterministic LangSmith-shaped `httptest` server. These cover `run list`, `run export`, `trace list`, `trace list --show-hierarchy`, `trace export`, and the exact `/runs/query` request bodies emitted by `--include-all` plus depth/agent/path filters.

The fixture-backed e2e tests are generated from real local execution of the CLI command path. Live LangSmith/OpenAI evidence is preserved separately under `reports/`.

Docs verification run from `langchain-docs/`:

```bash
git diff --check
```

The docs checkout is sparse and only contains selected docs files, so a full docs build was not run locally.

## Upstream Collision Check

Checked current open upstream work on May 29, 2026:

- `langchain-ai/langsmith-cli` has no open issue or PR that duplicates granular depth/agent/path trace querying.
- `langchain-ai/docs` has no open issue or PR matching `langsmith cli trace filter`.
- `langchain-ai/langsmith-cli` PR #143 (`feat(run): add --version v2 flag to runs query path`) touches `filters.go`, `helpers.go`, `run.go`, `extract.go`, `go.mod`, and `go.sum`. If it merges first, rebase the CLI patch and re-evaluate all selectable run fields against the newer `langsmith-go` generated types.
- `langchain-ai/langsmith-cli` PR #145 (`feat: add one-per-thread trace listing`) touches `helpers.go`, `trace.go`, and `trace_test.go`. If it merges first, rebase the CLI patch and adapt the `queryRuns` / `queryTraceRoots` call sites around the new request-option plumbing.

## Native LangSmith Backend/UI Follow-Up

If LangChain wants this feature natively in the hosted UI or indexed query backend, the LangSmith app/backend would need to add or expose:

- Server-side range filters or first-class aliases for hierarchy position, such as `min_depth`, `max_depth`, or non-metadata depth fields.
- Indexed filters for selected non-system metadata paths such as `extra.metadata.ls_agent_type`.
- Clear server semantics for arbitrary nested `extra`, `inputs`, and `outputs` paths beyond the documented filter DSL and key-value search limits.
- UI affordances for depth and agent/subagent filters in the trace and details views.

The CLI patch is still valuable even if LangChain later adds native backend support: it gives users and coding agents an immediate scriptable path and demonstrates concrete semantics for the backend/UI version.
